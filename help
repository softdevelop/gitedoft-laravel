1. Create database

2. Change content of file .env, (if not show, press: Ctrl+H)

database,
username,
pasword

example:
DB_DATABASE=films_laravel
DB_USERNAME=root
DB_PASSWORD=12345678

3. Type command:

php artisan key:generate
php artisan migrate
php artisan migrate:refresh --seed

4. Run server

php artisan serve


